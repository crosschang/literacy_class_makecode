```template
player.onChat("open", function (num1) {
    DoorControl.OpenDoor(num1)
})
player.onChat("close", function (num1) {
    DoorControl.CloseDoor(num1)
})
```

# MakeCode에 오신것을 환영합니다

## 지도 이미지에 맞는 채팅명령어를 적어주세요

```package
literacy_class_makecode_extension=github:crosschang/literacy_class_makecode_extension#v1.1.3
```